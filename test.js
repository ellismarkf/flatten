var expect = require('chai').expect

describe('flatten', function() {
	xit('should flatten an array of arbitrarily nested arrays of integers into a flat array of integers', function() {
		var nestedArray = [[1,2,[3]],4];
	});
});
